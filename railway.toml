[build]
builder = "NIXPACKS"
buildCommand = "curl -L -o pocketbase.zip https://github.com/pocketbase/pocketbase/releases/download/v0.22.13/pocketbase_0.22.13_linux_amd64.zip && unzip -o pocketbase.zip && chmod +x pocketbase"

[deploy]
startCommand = "./pocketbase serve --http=0.0.0.0:$PORT --dir=/data --migrationsDir=./pb_migrations --hooksDir=./pb_hooks --hooksWatch=false"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
